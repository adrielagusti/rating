<mvc:View
    controllerName="blackseeds.ratings.controller.Main"
    xmlns:html="http://www.w3.org/1999/xhtml"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:core="sap.ui.core"
    displayBlock="true"
    xmlns="sap.m"
>
    <App id="app">
        <Page
            showHeader="false"
            class="sapUiContentPadding"
        >
            <subHeader>
                <Toolbar>
                    <ToolbarSpacer />
                    <Select selectedKey="{userSelectionModel>/User}">
                        <items>
                            <core:Item
                                key="Level"
                                text="Level"
                            />
                            <core:Item
                                key="Pepe"
                                text="Pepe"
                            />
                            <core:Item
                                key="Chelok"
                                text="Chelok"
                            />
                        </items>
                    </Select>
                </Toolbar>
            </subHeader>
            <content>
                <List
                    id="idList"
                    items="{/Strains}"
                    selectionChange="onSelectRow"
                    mode="SingleSelectMaster"
                    growing="true"
                    growingThreshold="50"
                    includeItemInSelection="true"
                >
                    <!-- <infoToolbar>
                        <OverflowToolbar>
                         
                            <core:Icon src="sap-icon://shield"/>
                        </OverflowToolbar>
                    </infoToolbar> -->
                    <!-- icon="{ProductPicUrl}" -->
                    <CustomListItem
                        press="onSelectRow"
                        type="Active"
                        detailPress="onSelectRow"
                    >
                        <FlexBox
                            justifyContent="SpaceBetween"
                            alignContent="Center"
                            alignItems="Center"
                        >
                            <VBox>
                                <Title text="({tagID}) {name}" class="sapUiSmallMarginTopBottom"/>
                                <Text text="Add breeder" class="sapUiSmallMarginBottom"/>
                            </VBox>
                                    <RatingIndicator
                            maxValue="5"
                            value="{value}"
                        />
                        </FlexBox>
                
                    </CustomListItem>
                </List>
            </content>
            <footer>
                <Toolbar>
                    <ToolbarSpacer />
                    <Select
                        forceSelection="false"
                        selectedKey="{userSelectionModel>/Strains}"
                        type="IconOnly"
                        icon="sap-icon://filter"
                        autoAdjustWidth="true"
                        items="{
						path: '/Strains',
						sorter: { path: 'name' }
					}"
                    >
                        <core:Item
                            key="{stainID}"
                            text="{name}"
                        />
                    </Select>
                </Toolbar>
            </footer>
        </Page>
    </App>
</mvc:View>
